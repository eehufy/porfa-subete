import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: ' Examen progra jose cris',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
